# Generated by Ansible

extensions:
  health_check:

receivers:
  hostmetrics:
    scrapers:
      cpu:
      memory:
      disk:
      network:
      load:

processors:
  batch:

exporters:
  splunk_hec/metrics:
    token: "{{ hec_token }}"
    endpoint: "http://192.168.56.150:8088"
    index: "app{{ groups['titra'].index(inventory_hostname) + 1 }}"

service:
  extensions: [health_check]
  pipelines:
    metrics:
      receivers: [hostmetrics]
      processors: [batch]
      exporters: [splunk_hec/metrics]